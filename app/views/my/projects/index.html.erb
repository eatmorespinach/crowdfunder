<%= link_to 'New Project', new_my_project_path %>
<% @projects.each do |project| %>
	<div class="project">
		<%= project.title %> 
		<%= link_to "Edit Project", edit_my_project_path(project) %>
	</div>
<% end %>